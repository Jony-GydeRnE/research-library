You are converting an academic paper page image into clean, structured HTML for a web-based reader.

Rules:
1. Preserve ALL text content from the page exactly as written.
2. Wrap inline math in \( ... \) delimiters. Examples: \( X_{ij} \), \( c_{1i} = 0 \), \( \phi^3 \).
3. Wrap display/block equations in \[ ... \] delimiters on their own line. Include equation numbers like (8) inside the math block.
4. Use <h2> for major section headings (like "INTRODUCTION", "CONCLUSION").
5. Use <h3> for subsection headings (like "Amplitude zeros", "UV/BCFW scaling").
6. Use <p> for body paragraphs. Join text that flows as one paragraph even if it wraps across lines.
7. For figures/diagrams, output:
   <figure class="page-figure" data-bbox="LEFT%,TOP%,RIGHT%,BOTTOM%"><figcaption>FIG. N: caption text</figcaption></figure>

   BOUNDING BOX RULES (critical — read carefully):
   - LEFT%, TOP%, RIGHT%, BOTTOM% are percentages of the full page image (0–100).
   - The bbox frames ONLY the visual artwork: the drawn lines, shapes, graphs, plots, shading, and any labels/symbols that are physically part of the diagram (vertex labels like "i+1", axis labels, data points, arrows between sub-figures).
   - NEVER include the figure caption ("FIG. 1: ...", "Figure 3: ...") inside the bbox. The caption is BELOW the artwork. Your BOTTOM% must stop ABOVE the first line of caption text.
   - NEVER include body text paragraphs that come after the figure. If you see a sentence like "We need to prove..." or "three types of X_ij are given by" below the diagram, your BOTTOM% must be ABOVE that text.
   - NEVER include display equations that follow the figure. Equations are already captured as LaTeX in the HTML output — do not duplicate them in the image crop.
   - Think of it as: draw a tight rectangle around JUST the picture, as if cutting it out with scissors. Caption and paragraphs are outside the scissors line.
   - Add ~2% padding on each side beyond the outermost drawn element.
   - For two-column pages where a figure sits in one column, set LEFT% and RIGHT% to frame only that column's width.
   - For figures that span both columns, LEFT% ≈ 3–5% and RIGHT% ≈ 95–97%.

   Example: <figure class="page-figure" data-bbox="10.2,22.8,47.5,44.1"><figcaption>FIG. 1: The kinematic mesh organizes...</figcaption></figure>

   If you genuinely cannot localize the figure, omit data-bbox entirely — the reader falls back to a link to the original page.
8. Do NOT include page numbers, headers, footers, or arXiv identifiers.
9. Do NOT add any CSS, JavaScript, or wrapper divs.
10. Do NOT explain or comment — output ONLY the HTML.
11. Use standard LaTeX notation: subscripts _{}, superscripts ^{}, fractions \frac{}{}, summations \sum, etc.
12. For bold section labels within a paragraph (like "Locality" or "Unitarity"), use <strong>.
13. Preserve citation references like [15] as-is in the text.

Output the HTML content for this single page. Start directly with the first HTML element.